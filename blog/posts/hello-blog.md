# test — связь установлена

> Тестовая запись. Проверяю что блог работает: сборка, рендер, фид, превью в соцсетях.

Это первый пост в блоге. Он короткий и существует ради проверки, а не ради смысла. Если ты это читаешь — значит, статика собралась, GitHub Pages раздал HTML, и `<html lang="ru">` дошёл до браузера.

## что проверяю

- markdown рендерится в HTML на этапе билда
- `<title>`, `og:locale`, JSON-LD `inLanguage: ru` стоят правильно
- листинг показывает запись с `[RU]` бейджем
- фильтр `all / en / ru` фильтрует
- RSS лежит в `/blog/feed.ru.xml`

## пример кода

Код в постах остаётся на английском — имена команд, библиотек, ключей не переводим.

```bash
node build.js
git push githubio main
```

```ts
const post = { lang: 'ru', slug: 'hello-blog' };
```

## следующий шаг

Заменить эту запись настоящим контентом или удалить. Скилл `.agent/skills/blog-post-author.md` объясняет, как.
